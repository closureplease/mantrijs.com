doctype xml
feed(xmlns='http://www.w3.org/2005/Atom')
  title MantriJS Blog Feed
  link(href='http://mantrijs.com/rss', rel='self')
  link(href='http://mantrijs.com')
  if posts.length
    updated= new Date(posts[0].postRawDate).toISOString()
  id http://mantrijs.com/

  each post in posts
    entry
      title= post.title
      link(href="http://mantrijs.com/blog/#{post.url}")
      content(type='html')
        | <![CDATA[
        | !{post.rssSrc}
        p
          a(href='http://mantrijs.com/blog/#{post.url}', rel="nofollow") Read more
        | ]]>
      id= post.atomId
      updated= new Date(post.postRawDate).toISOString()
      author
        name Grunt Team
